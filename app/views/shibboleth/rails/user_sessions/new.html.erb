<%= form_tag user_session_path do %>
  Login as:
  <%= select_tag ::User.primary_key, options_from_collection_for_select(@users, ::User.primary_key.to_sym, :to_s), class: 'chosen autosubmit' %>
  <%= submit_tag "Login", :class => 'button' %>
<% end %>
